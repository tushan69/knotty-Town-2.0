# KNOTTY TOWN - HOSTINGER PRODUCTION CONFIG
DirectoryIndex index.html

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Force HTTPS
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  # Allow existing files and directories to load normally
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Routing specifically for API calls
  RewriteRule ^api/(.*)$ api/$1 [L]

  # Route everything else to index.html for React Router handling
  RewriteRule . index.html [L]
</IfModule>

# Browser Caching Rules for Performance Optimization
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType text/javascript "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

<IfModule mod_headers.c>
  <FilesMatch "\.(jpg|jpeg|png|webp|svg|css|js)$">
    Header set Cache-Control "max-age=2592000, public"
  </FilesMatch>

  # Security Hardening Response Headers
  Header set Strict-Transport-Security "max-age=31536000; includeSubdomains; preload"
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "DENY"
  Header set X-XSS-Protection "1; mode=block"
  Header set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://checkout.razorpay.com https://*.razorpay.com https://*.googleapis.com https://apis.google.com https://*.firebaseapp.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https: wss:; frame-src 'self' https://checkout.razorpay.com https://*.razorpay.com https://*.firebaseapp.com https://*.google.com;"
</IfModule>

# Security: Prevent directory listing

# PHP Performance Tuning
# <IfModule mod_php7.c>
#    php_value upload_max_filesize 64M
#    php_value post_max_size 64M
#    php_value memory_limit 128M
# </IfModule>
# <IfModule mod_php.c>
#    php_value upload_max_filesize 64M
#    php_value post_max_size 64M
#    php_value memory_limit 128M
# </IfModule>